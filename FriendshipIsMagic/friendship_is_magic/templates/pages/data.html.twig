{% extends 'base.html.twig' %}

{% block title 'DataController | FriendShipIsMagic' %}

{% block body %}
    <button type="button" class="btn btn-dark _back" style="position: absolute; top: 5px; left: 5px;"><a style="color: aliceblue" href="{{ path('home.index') }}">Return to Home</a></button>
    <div class="jumbotron text-center">
        <h3>Data Controller</h3>
    </div>
    <div class="container-fluid">
        <div class="row">
            <div class="col col-6">
                <div class="text-center">
                    <h3>Data Manager</h3>
                </div>
            </div>
            <div class="col col-6">
                <div class="text-center">
                    <h3>Data render</h3>
                    <div class="row flex mt-5">
                        {% for unicorn in allUnicorn %}
                            <div class="card" style="width: 18rem;">
                                <div class="card-body">
                                    <h5 class="card-title">{{ unicorn.nom }}</h5>
                                    <h6 class="card-subtitle mb-2 text-muted">{{  unicorn.modele }}</h6>
                                    <p class="card-text">Prix TTC: {{ unicorn.prix }} €</p>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}